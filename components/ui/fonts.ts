import { Inter, Lusitana, Pattaya } from 'next/font/google';

export const inter = Inter({ subsets: ['latin'] });
export const cursive = Pattaya({ subsets: ['latin'], weight:["400"] });
export const lusitana = Lusitana({ subsets: ['latin'], weight: ["400", "700"] });